"""Standalone modeling tools shipped with axiomize.

Each module is importable (``axiomize.tools.validate``) and also runs as a
script; the package exposes them as the ``axiomize-*`` console commands.
"""
