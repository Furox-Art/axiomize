# Security non-goals

Axiomize does not claim that a temporary directory, Python isolated mode, subprocess timeouts, or reduced environment inheritance constitute a hostile-code OS sandbox. Hostile arbitrary code must be run inside an external isolation boundary appropriate to the deployment environment.
